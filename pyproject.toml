[project]
name = "pdf-to-interactive-lesson-api"
version = "0.1.0"
description = "Python API endpoints for PDF processing"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

